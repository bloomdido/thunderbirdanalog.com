const listStyles = {
  listStyle: props => props.listStyle,
  listStyleImage: props => props.listStyleImage,
  listStylePosition: props => props.listStylePosition,
  listStyleType: props => props.listStyleType,
}

export default listStyles
